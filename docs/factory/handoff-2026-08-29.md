# Handoff — Mixtape feature factory, 2026-08-29 (end of day)

Paste the block below as the first message of the next session. Everything
it references is on `main` at `bac2109` or later.

---

Continue the Mixtape feature factory from where the last session stopped
(2026-08-29, evening). Read first, in this order: the memory file
`mixtape-feature-factory.md` (recalled automatically), `docs/factory/plan.md`
sections 4, M3–M5, 6 and 11 (every "Verified 2026-08-29" block is measured
fact — respect it), `docs/playbooks/run-the-factory.md`, `docs/factory/RUNS.md`,
`docs/reviews/0002-spec-panel-2026-08-29.md`, `CLAUDE.md`, `.claude/settings.json`,
`.claude/workflows/*.js`, `.claude/agents/*.md`, `scripts/factory-run.sh`,
`scripts/workflow-selftest.mjs`, `tools/console/README.md`.

State on main: M1–M5 done · console C1–C4 done (`npm run console` →
http://127.0.0.1:5174, local only) · two saved workflows:
`/implement-from-spec` (Implement → Gate → Review) and `/review-spec`
(Check → Clarity → Craft → Apply) · `scripts/workflow-selftest.mjs` is gate
step 6 · driver `scripts/factory-run.sh <spec>` (clean checkout → worktree
`../mixtape-poc.wt` → trust check → `claude -p` with `{spec, config}` from
`factory.config.json` → RUNS.md row + PR body) · `factory.config.json` has
`implementModel: "sonnet"` · dry run 1 = draft PR #1 (`factory/0001-share`,
$3.92, not merged) · `specs/0002-album-position-gate-blind-spots.md` is
`status: draft`, `touches_prompt: true`, with a panel review recorded in
`docs/reviews/0002-spec-panel-2026-08-29.md` (3 minor wrongs, 16 must-adds,
3 decisions for me). Workspace is trusted; the worktree is NOT yet trusted.

My decisions on spec 0002's three open points: [FILL IN — closer no-op:
edition words only / any parenthetical · possessive after a link word:
idiom / link · bundling: keep slices 4–6 / split].

Do, in order, committing and pushing to main after each verified stage:
1. Apply the spec-0002 edit set: either run `/review-spec
   specs/0002-album-position-gate-blind-spots.md` (its first real run —
   in-session, ~free; compare its findings to the recorded review, note
   the overlap in RUNS.md's notes) or, if I say "apply directly", have one
   subagent fold `docs/reviews/0002-spec-panel-2026-08-29.md` into the spec.
   Either way the spec stays `draft`; show me the diff; I flip it to ready.
2. Dry run 2: `scripts/factory-run.sh specs/0002-album-position-gate-blind-spots.md`.
   The first pass builds the worktree and exits 3 at the trust step; tell
   me to run `cd ../mixtape-poc.wt && claude` once; then run it again for
   real. Watch it in the console (start `npm run console` first, or check
   it is already listening on 5174). Expected end state: `ready-for-eval`,
   implementer on Sonnet — record the cost delta vs dry run 1 ($3.92,
   Fable) in RUNS.md. Validate the branch yourself (read the diff, run
   `npm run gate`) before the PR; the driver prints the PR command and the
   pre-merge checks. Do NOT merge, do NOT run the eval leg — both are mine.
3. If the run escalates (`needs-human`), stop and report the reason with
   the manifest and journal evidence.
4. Update the memory file and the plan's Verified blocks (M5 row count,
   dry run 2) when done.

Rules, unchanged: subagents implement, you orchestrate and validate every
stage yourself before committing; sequential agents, no parallel fan-outs
(read-only review panels excepted); never run paid evals; never edit
never-tier files — stage them at the repo root and give me a short `! mv`
or `! cp -R` line; never add a broad `Bash(*)` allow; report failures as
failures. Costs: a full line run is ~$3.9 on Fable, unknown on Sonnet —
tell me the total at the end. Stop for me only at: the three decisions if
I have not filled them in, the worktree trust step, `status: ready`, an
escalation, and the PR merge.
