Spec 0002: album-position-gate-blind-spots — `specs/0002-album-position-gate-blind-spots.md` through `scripts/factory-run.sh` on `factory/0002-album-position-gate-blind-spots`, no human between approval and review (row of 2026-08-30 in `docs/factory/RUNS.md`).

## Factory run

| | |
| --- | --- |
| workflow | `/implement-from-spec`, headless `claude -p --permission-mode acceptEdits --max-turns 60 --max-budget-usd 5` |
| run | `wf_2d570cca-1ac` · 4 agents · 197k tok · 11.2 min |
| attempts | implement / gate / review: 1 / 1 / 1 |
| gate | `npm run gate` passed (first run) |
| review | `reviewer`: pass, 0 findings (claude-sonnet-5) |
| cost | $2.93 (`total_cost_usd`) |
| implementModel | `sonnet` |

## Validated by hand before this PR

The orchestrator re-checked the branch rather than taking the reviewer's verdict on trust:

- `npm run gate` in the worktree — passed in 3 s (step 0 ask-tier check included)
- `cd server && npm test` — **125/125**, `+8` tests in `server/test/curator.test.ts` (61 → 69)
- the spec's eight slices are all present with its exact 7-row fixture table (track number, totals, `album_type`, `release_date` all match cell for cell), plus both guards the spec said must stay green: `pylon`'s wrong-album form and `disquedor`'s noun form
- the `SYSTEM` bullet is one physical line, as `/review-spec` required
- no ask-tier or never-tier path, no `package.json` change, no untracked strays

Three notes for the `/code-review` below — none of them blocked the PR:

1. **Tests and code were written together, not red-then-green per slice.** The run record says no fixture or rule needed a second try. Acceptance check 1 asks for one slice at a time, typed red first. Third factory run in a row where Sonnet does this; it is a property of the model, not a defect in the output.
2. **The noun-form album-name check compares the *normalized* last token.** `normalize` turns `Disque D'or` into `d or`, so the token actually matched is `or` — a common English word inside the 3 tokens before an `opener`/`closer` would read as album context. Narrow, but a latent false positive the reviewer did not flag. (Worth comparing against the discarded attempt 3, which solved the same problem with raw letters-only tokens: `docs/factory/runs/2026-08-29-0002-attempt3.diff`.)
3. **`status: implemented` is not in `specs/_template.md`'s value list** (`draft → ready → done | escalated`). The `/implement` skill prescribes it at step 8, so skill and template disagree. Cosmetic.

## Cost, read honestly

$2.93 splits as $1.91 sonnet (implement, gate, contract, reviewer) + **$1.02 opus-5[1m]** at the top level: `claude -p` inherits the CLI's default model, and a `/model` in an interactive session that morning moved orchestration off fable-5, where attempts 1–3 ran. So $2.93 against dry run 1's $3.92 is **not** a clean Sonnet-vs-Fable comparison — plan §6 item 3's like-for-like row is still owed.

One permission denial, harmless: an agent tried a compound `git status && echo … && node -e …` line, the exact-string allowlist refused it, and the agent recovered. The allowlist working as designed.

## Pre-merge, by a human

- [ ] `/code-review factory/0002-album-position-gate-blind-spots since main` — Standards (the repo's documented conventions plus the smell baseline) and Spec (against `specs/0002-album-position-gate-blind-spots.md`); the factory reviewer judged the contract only and was told not to grade style
- hand checks: (none in the spec)

Do not merge from the agent side — the merge is the human gate.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_013gws7xggrmqNrtGmAJgxgV
