{
  "name": "mixtape-poc-server",
  "version": "0.1.0",
  "private": true,
  "description": "Express server for the mixtape POC: Spotify OAuth + Claude curator",
  "type": "module",
  "main": "index.ts",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node --watch index.ts",
    "start": "node index.ts",
    "test": "node --test",
    "typecheck": "tsc -p .."
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.60.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
